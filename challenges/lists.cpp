#include "lists.hpp"

#include <utility>

adt::SinglyLinkedList<int> chl::AddNumbers(adt::SinglyLinkedList<int> /*l1*/,
                                           adt::SinglyLinkedList<int> /*l2*/) {
  // auto AddNumbers = [](int rhn, int lhn) {
  //   auto res = lhn + rhn;
  //   auto carry_over = 0;
  //   if (res >= 10) {
  //     carry_over = 1;
  //     res -= 10;
  //   }
  //   return std::make_pair(res, carry_over);
  // };

  return adt::SinglyLinkedList<int>();
}
